<div class="window d-flex">
  <div class="search-container">
    <div class="search-title">
      <h2>Filter your search for kitas</h2>
    </div>

    <div class="form-container">
      <%= form_tag kindergartens_path, method: :get do %>
        <%= text_field_tag :address,
          params[:address],
          class: "form-control",
          placeholder: "What's your address?"
        %>
        <p>Search radius (in km):</p>
        <%= select_tag :radius,
          "<option>1</option>
          <option>2</option>
          <option>3</option>
          <option>4</option>
          <option>5</option>". html_safe, class:"form-control" %>

        <%= submit_tag "Search", class: "btn-pib mt-2" %>
      <% end %>
    </div>
  </div>

  <div
    id="map"
    style="width: 75%;
    height: 100vh;"
    data-markers="<%= @markers.to_json %>"
    data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>"
  ></div>
</div>
